<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.dwq.orderingSys.data.dao.UserMapper">
    <resultMap id="BaseResultMap" type="indi.dwq.orderingSys.data.pojo.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"   />
        <association property="role" javaType="java.lang.String" select="indi.dwq.orderingSys.data.dao.RoleMapper.selectNameById"   column="role"/>
    </resultMap>
    <sql id="Base_Column_List">
    <![CDATA[ id, username, password ]]>
  </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
       <![CDATA[ select id, username, password ,role  from user where id = #{id,jdbcType=INTEGER} ]]>
    </select>
    <select id="findByUserName" parameterType="java.lang.String" resultMap="BaseResultMap">
        <![CDATA[ SELECT *
        FROM USER  WHERE username = #{username} ]]>
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        <![CDATA[ delete from user
        where id = #{id,jdbcType=INTEGER} ]]>
  </delete>
    <insert id="insert" parameterType="indi.dwq.orderingSys.data.pojo.User">
    insert into user (id, username, password
      )
    values (#{id,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}
      )
  </insert>
    <insert id="insertSelective" parameterType="indi.dwq.orderingSys.data.pojo.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="username != null">
                #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="indi.dwq.orderingSys.data.pojo.User">
        update user
        <set>
            <if test="username != null">
                username = #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="indi.dwq.orderingSys.data.pojo.User">
    update user
    set username = #{username,jdbcType=VARCHAR},
      password = #{password,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
</mapper>